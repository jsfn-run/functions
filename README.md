# Public functions for JS-FN project

These functions are available at [`jsfn.run`](https://jsfn.run)

## Usage

```sh
docker run --rm -d --name functions -e REPOSITORY=jsfn-run/functions -e MULTIPLEXED=true
```
